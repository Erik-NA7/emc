{
  "name": "entreprenur-mahasiswa-hindu",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "eleventy",
    "export": "next build && next export",
    "start": "eleventy --serve",
    "lint": "next lint"
  },
  "dependencies": {
    "clean-css": "^5.3.3"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}